apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: gamehub-backend
spec:
  template:
    spec:
      containers:
        - image: us-docker.pkg.dev/cloudrun/container/gamehub-backend
          ports:
            - name: http1
              containerPort: 80